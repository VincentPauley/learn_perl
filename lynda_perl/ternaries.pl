use 5.18.0;
use warnings;

my $x = 1;
my $y = 2;

# ternary
say $x > $y ? 'true' : 'false';

# EXPLANATION
#
# if the condition is true, return the first value, if the expression is false,
# return the second value (after the colon)
