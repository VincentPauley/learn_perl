# shabang line, used to tell unix where the interpreter is
# hello.pl by Vincent Pauley

use 5.18.0; # tells perl to use syntax from version 18, new features can be added to perl without affecting older builds.

use warnings; # recomended, provides errors

say "Hello, World!"; # say includes a new line as opposed to 'print'
