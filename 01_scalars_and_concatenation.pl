# scalars can be undefined, a number, a string, or another variable.  They are defined with $

# vars are declared with 'my'
my $name = "Vincent";

# .syntax to concatenate, here a scalar variable is combined with a string
print $name." is a programmer";
